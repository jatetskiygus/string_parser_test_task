<Window x:Class="test_task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:test_task"
        xmlns:vm="clr-namespace:test_task.ViewModels"
        Title="Text Analyzer" Height="300" Width="500">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Стили кнопок -->
        <Style TargetType="Button" x:Key="FancyButtonStyle">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderBrush="Transparent" 
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3399FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" 
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Height="80"
                 Margin="0,0,0,10"
                 FontSize="14"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Анализировать"
                    Command="{Binding AnalyzeCommand}"
                    Style="{StaticResource FancyButtonStyle}"
                    Background="#FF007ACC"/>

            <Button Content="Очистить"
                    Style="{StaticResource FancyButtonStyle}"
                    Background="Gray"
                    Command="{Binding ClearCommand}"/> 
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="7,26,0,88" Grid.RowSpan="2">
            <Button Command="{Binding AddFileCommand}"
                    Width="36" Height="31"
                    ToolTip="Прикрепить файл">
                <TextBlock FontSize="24" 
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center">
                               📎
                </TextBlock>
            </Button>
            <TextBlock x:Name="FileNameTextBlock" Text="{Binding SelectedFileName}" Width="100" TextAlignment="Center" VerticalAlignment="Center" FontSize="20" />
        </StackPanel>
        <TextBlock Grid.Row="2" 
                   Text="{Binding AnalysisResult}" 
                   Margin="7,42,0,0"
                   TextWrapping="Wrap" 
                   FontSize="14"
                   Foreground="DarkSlateGray"/>
    </Grid>
</Window>
