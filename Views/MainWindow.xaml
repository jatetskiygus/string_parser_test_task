<Window x:Class="test_task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:test_task"
        Title="Text Analyzer" Height="300" Width="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputTextBox"
                 AcceptsReturn="True"
                 Grid.Row="0" 
                 Height="80"
                 Margin="0,0,0,10"
                 FontSize="14"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="AnalyzeButton"
                    Content="Анализировать"
                    Click="Analyze"
                    Background="AliceBlue"
                    Foreground="Black"/>

            <Button Content="Очистить"
                    Background="Gray"
                    Click="Clear"/> 
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,5,0,0" VerticalAlignment="Top">
            <Button x:Name="AddFileButton" 
            Click="AddFile"
            Width="36" Height="31"
            ToolTip="Прикрепить файл">
                <TextBlock FontSize="24" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center">
                   📎
                </TextBlock>
            </Button>
            <TextBlock x:Name="FileNameTextBlock"  Width="100" TextAlignment="Center" VerticalAlignment="Center" FontSize="20" />
        </StackPanel>
        <TextBlock x:Name="AnalysisResultTextBox"
                   Grid.Row="3"  
                   Margin="7,42,0,0"
                   TextWrapping="Wrap" 
                   FontSize="14"
                   Foreground="DarkSlateGray"/>
    </Grid>
</Window>
